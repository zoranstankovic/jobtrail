; Loaded after php.ini-production (docker/app/Dockerfile, stage app).
; The code in the image never changes, so OPcache never re-checks files.
opcache.enable=1
opcache.validate_timestamps=0
opcache.memory_consumption=128
opcache.max_accelerated_files=20000
expose_php=Off
